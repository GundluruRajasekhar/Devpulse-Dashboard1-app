# Devpulse-Dashboard1-app
Dashboard homepage — app/page.tsx composes a profile panel, stat tiles, Projects, and Tasks into one landing view. Nav bar — sticky top nav with aria-current active states, a skip-to-content link, keyboard-accessible mobile menu, and an in-scope "Reports" item marked "Soon" (a real roadmap signal, not a fake feature).
